apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: thrive-ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: thrive_ai
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: thrive-ai
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
